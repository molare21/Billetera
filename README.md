# AL DÍA

Aplicación web de billetera personal.

## Producción
El frontend y la API PHP se despliegan desde GitHub.

Las credenciales MySQL NO se guardan en el repositorio.
En el servidor debe existir, fuera de `public_html`:

`private/aldia-db.php`

Usa `api/config.example.php` como plantilla.
